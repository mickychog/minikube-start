apiVersion: v1
kind: Service
metadata:
  name: {{ include "hola-chart.fullname" . }}
  labels:
    {{- include "hola-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hola-chart.selectorLabels" . | nindent 4 }}
